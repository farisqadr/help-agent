services:
  help-agent:
    build: .
    ports:
      - "4321:4321"
    env_file:
      - .env
    volumes:
      - ./positions.json:/app/positions.json
      - ./trade-history.json:/app/trade-history.json
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
